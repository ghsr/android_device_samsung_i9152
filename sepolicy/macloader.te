type macloader, domain;
type macloader_exec, file_type, exec_type;

init_daemon_domain(macloader)

allow macloader wifi_efs_file:dir rw_dir_perms;
allow macloader wifi_efs_file:file rw_file_perms;

allow macloader self:capability { sys_module net_raw net_admin dac_override };
allow macloader self:udp_socket { create ioctl };
